# PostgreSQL
DB_DRIVER=org.postgresql.Driver
DB_URL=jdbc:postgresql://localhost:5432/helpdeskdb
DB_USER=helpdesk_user
DB_PASSWORD=change_me

# JWT secret (replace with secure 32+ byte secret)
JWT_SECRET=change-this-secret-to-a-strong-key

# App server port (if needed)
PORT=8080